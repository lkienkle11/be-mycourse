## 0. Workflow compliance bootstrap (AGENTS.md Implementation Workflow Rules)

- [x] 0.1 Read the complete existing project `.context` folder (every session file, not only the ones judged relevant by a keyword search) for prior decisions, conventions, and failures relevant to this change, and verify by naming which files were read. Done: all 19 pre-existing session files read (01457896, 05188613, 1e75c950, 2ee802b2, 536c6e8a, 611a23d9, 61dbaa9b, 6d6e1382, 751407ec, 7e8f43a7, 898a4a79, a022710f, accf7ff0, cc09c1ec, e3a9f879, e56de0bc, e7d53670, e98a7940, efe9242f). Key finding: Course was previously fully wired into `internal/authorization` (Aug 20-21, sessions 751407ec/05188613/536c6e8a) with a working `CoursePolicyProvider`, `course_basic_info:update`/`course_outline:update` actions, and an `EffectiveActionProjector`-driven collaborator response, then deliberately reverted before ever being committed (session e56de0bc, Sep 10) in favor of the generic role gate this change now builds on. The "owner actions are not hardcoded, computed via the projector so explicit DENY still overrides" pattern from that prior (reverted) work confirms this change's design decision (owner as a synthesized `PolicyAllow`) is compatible with DENY precedence. `scripts/generate-apidog-postman.rb` is the correct path (not root `generate-apidog-postman.rb`) per every session that regenerated it.
- [x] 0.2 Read the full content of every skill under `.ai/skills` (`cache-strategy-implementer`, `logic-n-1-optimize`, `postgresql-optimization`) and record which, if any, apply to this change, and verify by naming the applicability conclusion for each. Done: `cache-strategy-implementer` not applicable (no caching layer introduced). `postgresql-optimization` not applicable (no slow query; backfill is 25 rows, existing indexes cover the wildcard lookup). `logic-n-1-optimize` IS applicable: the batch collaborator endpoint must not be re-pointed to a single-item `RoleBindingService` method called in a loop. Design/spec updated accordingly (see design.md's batch decision and the new "Assignment and revocation accept multiple principals in one call" requirement in the role-binding-management spec).
- [x] 0.3 Initialize and use `memory-bank-cli` for this project (`be-mycourse`) per the Global Memory Bank Rules: run `memory_bank_read_all --project be-mycourse` before further task work, and from this point on persist `activeContext.md`/`progress.md` and the verbatim `lastResponse.md` after every response, and verify by confirming the read/write calls succeed. Done: `memory_bank_read_all` returned empty (no prior memory bank content existed for this project — confirmed none of the 19 `.context` sessions ever used this system). Initialized `activeContext.md` and `progress.md` via `memory_bank_write`; both confirmed written via `list_project_files`.
- [x] 0.4 Confirm the external Session ID bootstrap (`AI_SESSION_ID_FILE` outside the project) and the project `.context/session-<SESSION_ID>.md` file are in place, and verify both paths resolve and the session ID matches across them. Done: both verified present and matching (`3da1b83c-d069-4919-ac66-0d42de8ace68`).

## 1. Wildcard resource support in the role gate

- [x] 1.1 Change `GormGrantRepository.roleExpandedGrants`'s resource filter in `internal/authorization/infra/gorm_repository.go` from `rb.resource_id = ?` to `(rb.resource_id = ? OR rb.resource_id = '*')`, and verify with a unit test that a wildcard binding resolves grants for an arbitrary resource ID of its type, and that a wildcard for one resource type grants nothing for another. Done: added `domain.WildcardResourceID`, extracted `roleBindingResourceClause` (unit-testable without a DB, matching this file's existing no-DB-harness test convention) and wired it into `roleExpandedGrants`; the resource_type filter (untouched, already a separate AND clause) is what prevents cross-type leakage. `go build ./...`, `go vet ./...`, `go test ./internal/authorization/...` all pass.
- [x] 1.2 Add a migration documenting/enforcing the `'*'` sentinel on `authorization_role_bindings.resource_id` (comment and/or `CHECK` constraint per design.md's Open Question), and verify `make check-all` and the migration test suite (`migrations/authorization_base_test.go`-style) pass. Done: **as a new migration `000035_authorization_role_binding_wildcard`, not an edit to `000034` in place** — `000034` is already applied to this dev DB (version 34, confirmed earlier this session), so design.md's original assumption that it could be edited in place no longer holds. Added a `CHECK` constraint (`resource_id = '*'` OR UUID-shaped) plus a static text test mirroring `authorization_base_test.go`'s convention. Verified against the real local dev DB: applied up (version 35, constraint present via `psql \d`), tested down (version 34, constraint gone), re-applied up (version 35). `make check-all` passed clean (tests, vet, golangci-lint 0 issues, layout, architecture, dupl 0 groups, build-nocgo, build).
- [x] 1.3 Add a unit test asserting a resource-specific binding and a wildcard binding for the same principal/resource type merge without conflict (union of actions), matching `role-binding-wildcard` spec's combination requirement. Done: `TestMergeGrantsUnionsWildcardAndResourceSpecificBindings`.
- [x] 1.4 Add a unit test asserting revoking a wildcard binding removes its blanket access on the next decision (no per-resource state to clean up). Done: `TestMergeGrantsDropsRevokedWildcardImmediately` (models roleExpandedGrants's revoked_at IS NULL filter already excluding a revoked binding from its output, so mergeGrants sees nothing left to clean up).

## 2. Role-binding management write path

- [x] 2.1 Define the domain contract for a role-binding write path in `internal/authorization/domain` (assign/revoke), following `GrantService`'s existing shape but scoped to `authorization_role_bindings` fields only (no `action_name`/`conditions`/`valid_from`/`expires_at`). Per `.ai/skills/logic-n-1-optimize` (task 0.2): both methods take `PrincipalUserIDs []string` (a batch), mirroring `GrantReplacement`, not one principal per call. Done: `RoleBindingAssignment`/`RoleBindingRevocation`/`RoleBindingQuery`/`RoleBindingRepository` added to `internal/authorization/domain/authorization.go`.
- [x] 2.2 Implement `RoleBindingService` in `internal/authorization/application`, gating assign/revoke through the resource's `PolicyProvider.CanManageGrants` (including the wildcard case, gated at the resource-type level), and verify with unit tests for authorized and unauthorized issuers, and for a batch of principals in one call. Done: `internal/authorization/application/role_binding_service.go`; unit tests in `role_binding_service_test.go` using a fake `RoleBindingRepository` (mirroring `GrantService`'s existing `memoryGrantRepository` test pattern) cover batching, wildcard, unauthorized issuer, missing global boundary permission, revoke, revoke-already-revoked-is-a-no-op, and `RevokeResource` (added in task 5.3, see below).
- [x] 2.3 Implement the corresponding repository methods on `GormGrantRepository` (or a new role-binding-specific repository) for assign/revoke using one batched insert/update statement for the whole principal list (matching `CreateInBatches`/`grantInsertBatchSize`, not a per-principal query loop), and verify with unit tests covering: successful assignment records the issuing principal, revoking an already-revoked binding is a no-op per the spec, and a multi-principal batch issues one batched statement. Done: `AssignRoleBindings`/`RevokeRoleBindings` added to `GormGrantRepository`; the "records issuing principal"/"batch in one call"/"already-revoked is a no-op" behaviors are covered at the `RoleBindingService` test layer (task 2.2), matching this file's existing convention of testing GORM query-building logic indirectly since no live DB test harness exists here.
- [x] 2.4 Run `go build ./...`, `go vet ./...`, and the new package's tests; confirm no other package yet depends on this service (Course wiring is a later task group). Done: all pass; `grep -rn "RoleBindingService" --include="*.go" .` confirms zero callers outside `internal/authorization/application` itself (at the time this task was done; Course wiring in task groups 3-5 added callers as expected).

## 3. Course as a registered PolicyProvider

- [x] 3.1 Enumerate every current call site of `requireCourseAccess`, `requireEditorAccess`, and `requireOwnerAccess` across `internal/course/**`, and record the specific action each site is actually gating (owner-only vs. any-collaborator), to be named explicitly in step 3.3. Done: 13 call sites across 7 files. Any-collaborator: `repo_access.go:15` (loadCourseDetail → `course_detail:view`), `repo_access.go:270`/`repo_outline.go:466` (ensureEditableDraft, AcquireLease → `course_draft:edit`/`course_lease:manage`), `repo_collaborators.go:76` (ListCollaborators → `course_collaborators:view`), `repo_review.go:219` (ListReviewHistory → `course_review:view`). Owner-only: `repo_instructor.go:110,186,206` (PrepareDraft, DeleteCourse, RemoveCollaborator), `repo_collaborators.go:115` (ListInstructorCandidates), `repo_collaborators_bulk.go:132` (AddCollaboratorsBulk) → `course_collaborators:manage`/`course_draft:prepare`/`course_lifecycle:delete`; `repo_versioning.go:29` (updateDraftStatus), `repo_review.go:19` (ReopenDraft) → `course_review:manage`.
- [x] 3.2 Seed `authorization_role_actions` (via migration or application-level seed, per existing `UpsertActions` pattern) with today's OWNER and EDITOR action sets for `resource_type = "course"`, matching the actions enumerated in 3.1 exactly, and verify by asserting the seeded rows against the enumerated action list. Done: **application-level seed, not a migration** — a migration would need to reference `authorization_actions` rows that don't exist until `SyncActions` runs at app startup (FK ordering), so the only workable option was app-level (`GrantRepository.SyncRoleActions`, new method added to the `GrantRepository` interface, task 3.2 required adding this since it wasn't scoped in task group 2). `courseapp.RoleActions()` declares the 9 tuples; `wireAuthorization` calls `SyncRoleActions` after `SyncActions`. Verified live against dev DB: OWNER=9, EDITOR=5 role_action rows, matching 3.1's enumeration exactly.
- [x] 3.3 Implement `CoursePolicyProvider` in `internal/course` declaring these actions, with `Evaluate` returning `PolicyAllow` when `courses.owner_user_id == principal.ID` (synthesized owner access, no stored binding row), and verify with unit tests for the owner-allow path and the non-owner passthrough-to-grants path. Done: `internal/course/application/authorization_policy.go` + `authorization_policy_test.go` (owner-allow, non-owner-neutral-passthrough, missing-facts rejection, CanManageGrants owner-only, action/resource-type consistency, RoleActions exactly matches today's OWNER/EDITOR behavior).
- [x] 3.4 Register `CoursePolicyProvider` in `internal/server/wire.go`/`wire_course.go`, and verify the application boots (`go run .`) with the provider registered and zero behavior change for existing requests (course_collaborators still authoritative at this point — the access-check switch itself is flipped in task group 4). Done: `wireAuthorization` now returns `*AuthorizationServices{Authorizer, RoleBindingService}`; `wire.go` registers `courseapp.NewCoursePolicyProvider()` with `courseapp.RoleActions()`; `wireCourse` passes the services into `courseinfra.NewGormRepository`. Verified live (`MIGRATE=1 go run .`): boots clean, 9 `authorization_actions` rows for `course`, 14 `authorization_role_actions` rows, server listens on :8080.

## 4. Collapse course access checks into one seam

- [x] 4.1 Replace `requireCourseAccess`/`requireEditorAccess`/`requireOwnerAccess` in `internal/course/infra/repo_access.go` with one helper that calls `Authorizer.Authorize` for a specific action name (per the action map built in 3.1/3.2), and verify no other file in `internal/course` constructs an `AuthorizationRequest` or compares a role name directly (grep for `.Role ==`, `.Role !=`, `CollaboratorRoleOwner`, `CollaboratorRoleEditor` outside this one seam). Done: added `requireCourseAction` (the one seam, constructs the single `AuthorizationRequest` in `internal/course`) plus `courseAuthorizationPrincipal` (reads `requestprincipal.FromContext`). `requireCourseAccess`/`requireEditorAccess`/`requireOwnerAccess` kept as thin named wrappers (readability at call sites) that all delegate to it. Grep confirms every remaining `CollaboratorRoleOwner`/`CollaboratorRoleEditor`/`.Role` reference outside `_test.go` is either display-data assignment (`Role: domain.CollaboratorRoleOwner`) or `RoleActions()` declarative seed data — none are a security-decision comparison.
- [x] 4.2 Update every call site enumerated in 3.1 to pass its specific action name through the new seam, and verify the existing Course test suite (access-control tests for OWNER/EDITOR behavior) passes unchanged in outcome. Done: all 10 remaining call sites (across `repo_access.go`, `repo_outline.go`, `repo_collaborators.go`, `repo_instructor.go` ×3, `repo_versioning.go`, `repo_collaborators_bulk.go`, `repo_review.go` ×2) updated to pass their action from task 3.1's mapping. No existing course test exercises this path (no live DB harness), so "passes unchanged" was verified as "nothing broke": `go test ./internal/course/...` and full `make check-all` both pass.
- [x] 4.3 Remove the now-unused role-comparison logic and, once no code references them, the `CollaboratorRoleOwner`/`CollaboratorRoleEditor` constants' role-gate usage (constants may remain if still used for display/seed data), and verify `make check-all` passes. Done: the `if access.Role != domain.CollaboratorRoleOwner` comparison is gone; constants remain (display + `RoleActions()` seed data, as allowed). `make check-all` passes clean: tests, vet, golangci-lint 0 issues, layout, architecture, dupl 0 groups (after deduplicating two test pairs golangci-lint flagged), both builds.

## 5. Migrate collaborator data and reads onto the role gate

- [x] 5.1 Write a one-time backfill (migration or CLI script) inserting one `authorization_role_bindings` row per active `course_collaborators` row, and verify row counts match (25 active rows in the current dev DB) and each backfilled binding resolves the same actions the corresponding collaborator has today. Done, **executed before task group 4** (not after, despite the numbering): flipping the access-check switch in group 4 before backfilling would deny every existing EDITOR collaborator (no role_bindings rows would exist for them yet). Migration `000036_backfill_course_collaborator_role_bindings`: 8 EDITOR rows backfilled (of 25 active `course_collaborators` rows, 4 are OWNER-role display rows correctly excluded per design.md's synthesized-owner decision, matching the 8 EDITOR count seen in the dev DB). Static migration tests added; verified live against the dev DB.
- [x] 5.2 Re-point `ListCollaborators` (`internal/course/infra/repo_collaborators.go`) and `instructorCandidatesBaseSQL`'s exclusion subquery to read from `authorization_role_bindings` (joined to `users`/`media_files` as today), and verify existing collaborator-listing and instructor-candidate tests pass against the new source. Done: `collaboratorsSelectSQL` now sources from a `UNION ALL` of the synthesized owner (`courses.owner_user_id`) and active EDITOR role bindings; `instructorCandidatesBaseSQL`'s exclusion subquery does the same. Also re-pointed `ListEditableCourses` (`repo_instructor.go`), a 4th read site task 3.1 missed because it doesn't go through `requireCourseAccess`/etc. — found by grepping every remaining `course_collaborators` reference. `ListCollaborators`'s own access check was also using the wrong action (`requireCourseAccess`/`course_detail:view` instead of `course_collaborators:view`); fixed. `go test ./internal/course/...` and `make check-all` pass.
- [x] 5.3 Re-point collaborator write flows (`service_collaborators_bulk.go`, `handler_instructor.go`) to call `RoleBindingService` (assign/revoke) instead of writing `course_collaborators` rows, and verify the existing bulk-collaborator test suite passes against the new write path. Done, with one important correction found mid-implementation: **`RoleBindingService` cannot participate in `internal/course`'s GORM transactions** — `internal/authorization`'s `GormGrantRepository` owns its own `*gorm.DB` handle (constructed once in `wire_authorization.go`), not the caller's `tx`. This is exactly the cross-repository transaction contract a prior (reverted, uncommitted) design built and then removed "until a real production caller exists" (`.context/session-7e8f43a7-...md`) — this change is now that caller, but re-adding that contract is out of scope here. Fix applied: `AddCollaboratorsBulk`/`RemoveCollaborator`/`DeleteCourse`/`PermanentDeleteCourse` now run their course-side validation in one transaction, then call `RoleBindingService.Assign`/`Revoke`/`RevokeResource` (new method, mirrors `GrantService.RevokeResource`'s no-CanManageGrants-check exemption for lifecycle cleanup) sequentially after it commits, with the gap documented in code comments. Also removed the now-dead OWNER-role `collaboratorRow` insert at `CreateCourse` (nothing reads it anymore) and the `collaboratorRow` case in `softDeleteCourseTree`. `planBulkCollaboratorWrites` and its existing tests untouched (still pure, still pass). **Post-implementation `/code-review` fix**: `RemoveCollaborator` originally hardcoded `RoleName: domain.CollaboratorRoleEditor` on its revoke call, so a collaborator holding any other stored role (e.g. one created via the bulk-add endpoint's `role: "OWNER"` input, which the DTO's `oneof=OWNER EDITOR` validation permits and `RoleActions()` maps to full owner-equivalent actions) could never be removed through the API, a real regression versus the prior `course_collaborators` code, which deleted by `(course_id, user_id)` regardless of stored role. Fixed by letting `RoleBindingService.Revoke` accept an empty `RoleName` (meaning "every active role this principal holds on the resource"), while `Assign` still rejects an empty `RoleName` unchanged; `RemoveCollaborator` now omits `RoleName` entirely. `validateRoleBindingShape` split so the role-name-required check applies to `Assign` only. Added `TestRoleBindingServiceRevokeWithEmptyRoleNameEndsEveryRoleForPrincipal` and `TestRoleBindingServiceAssignRejectsEmptyRoleName`; `make check-all` passes clean. **Second post-review fix (2 more `/code-review` findings, both confirmed and fixed on explicit user go-ahead)**: (1) the "cross-repository transaction" gap above turned out not to be a genuine limitation — `wireAuthorization`/`wireCourse` both receive the same `*gorm.DB` — so added `internal/shared/gormx/tx_context.go` (`WithTx`/`DBFromContext`) letting a repository that only receives a `context.Context` join the caller's transaction; `GormGrantRepository.dbForContext` now checks it first. `AddCollaboratorsBulk`/`RemoveCollaborator`/`DeleteCourse`/`PermanentDeleteCourse` now call `RoleBindingService` from inside their course-side transaction via `gormx.WithTx(ctx, tx)`, so the whole operation is atomic. (2) `collaboratorsSelectSQL`'s `UNION`, `ListEditableCourses`'s `LEFT JOIN`, and `existingActiveCollaboratorBindings`'s `Table("authorization_role_bindings")` all hand-wrote SQL directly against that table, contradicting `RoleBindingRepository`'s own doc comment. Added the one sanctioned read path, `RoleBindingRepository.ListRoleBindings`/`RoleBindingService.List`, and re-pointed those three plus `instructorCandidatesBaseSQL`'s exclusion subquery to fetch their (small, resource- or principal-scoped) id/role set through it instead. `ListCollaborators`/`loadCollaborators`/`loadCollaboratorsByUserIDs` restructured around a new `collaboratorSourceRows` helper (owner + active role bindings, sorted owner-first-then-grant-order) and a `loadCollaboratorProfiles` step that joins/searches/filters against `users`/`media_files` only; pagination now happens in Go over the (small, per-course) matched set via `paginateCollaborators`. Added `TestSortCollaboratorSourcesOwnerFirstThenGrantOrder`, `TestPaginateCollaborators`, `TestCourseInfraSQLNeverReferencesAuthorizationRoleBindingsDirectly`, and a `RoleBindingService.List` unit test. `make check-all` and `openspec validate --strict` both pass; **no live-DB smoke test was possible this time** (the dev Postgres instance was not reachable in this session), so this is a known follow-up: re-verify `ListCollaborators`/`ListInstructorCandidates`/`ListEditableCourses` against the real dev DB before the next deploy, the way task 5.4 did for the original cutover. **Third post-review round** (a second, independent `/code-review` pass) found and fixed 4 more issues: (1) `requireCourseAction`'s own display-role lookup (`repo_access.go`) did an exact `resource_id` match, so a wildcard-bound principal was `Authorize()`-allowed then immediately denied by this follow-up query — fixed with the same wildcard OR-clause `roleExpandedGrants` uses. (2) `ListRoleBindings` (added in the second round) had the identical exact-match gap, meaning a wildcard-bound EDITOR would not appear in `ListEditableCourses`/`ListCollaborators`/the instructor-candidate exclusion set — fixed; `RevokeRoleBindings` is deliberately left exact-match-only (documented) since revoking one resource's access must never also revoke a wildcard binding's access to every other resource of that type. (3) Migration `000036`'s down script deleted every course EDITOR binding, not just the ones it backfilled — fixed by scoping the `DELETE` to `granted_by_user_id IS NULL` (every app-created binding always records the issuing principal there). (4) A doc-comment edit in the second round had merged `List`'s doc comment onto `validateRoleBindingShape` (missing blank line) — separated back out. **Not fixed, left for explicit user decision**: `AddCollaboratorsBulk` computes `plan.updateIDs` (an already-collaborator being re-added with a different role) but never acts on it, so a role change via bulk-add silently no-ops; the DTO's `oneof=OWNER EDITOR` also still lets a client request `role="OWNER"` even though owner access is never a stored binding. Both point at the same root cause (bulk-add should probably only ever accept `role=EDITOR`), but changing accepted API input values is a product/contract decision, not made unilaterally here. Also left open, all pre-existing from earlier task groups and independently confirmed real but lower-priority: `RoleBindingService.Assign`/`Revoke` calls hardcoding `IsOwner: true` (relies on an unenforced call-ordering invariant), the new global-RBAC-boundary-permission requirement on every course access check (a latent lockout risk if `course:read/update/delete` is ever unbound from the instructor role), `ListCollaborators`'s Go-side pagination (an accepted trade-off for small per-course collaborator counts), `Authorizer.authorize()` running an unconditional `ListActive` query even when the provider already resolved `PolicyAllow` (pre-existing, unrelated to Course), and `requireRoleBindingManager` duplicating `GrantService`'s boundary-permission logic (already flagged, accepted duplication). `make check-all` and `openspec validate --strict` both pass after this round. **Fourth round (explicit user go-ahead: "làm luôn đi")**: closed the "not fixed" item from the third round. `addCollaboratorsBulkRequest.Role`'s binding tag narrowed from `oneof=OWNER EDITOR` to `oneof=EDITOR` (`internal/course/delivery/dto.go`) — a client requesting `role: "OWNER"` now gets a 400 at the HTTP boundary instead of silently being accepted and ignored. This makes `plan.updateIDs` conclusively, permanently dead (not just currently unused): with `role` always `EDITOR`, an "already active" collaborator can never legitimately need a role change through this endpoint. Removed it along with the now-pointless `collaboratorRow` carrier type: `existingActiveCollaboratorBindings` renamed to `existingActiveCollaboratorUserIDs` and returns a plain `map[string]struct{}` (membership only), `planBulkCollaboratorWrites`'s `existingByUser map[string]collaboratorRow` param became `existingUserIDs map[string]struct{}`, `bulkCollaboratorWriteState.updateIDs` field removed, `collaboratorRow` struct deleted from `repos.go`. Updated `repo_collaborators_bulk_test.go` accordingly. `make check-all` passes clean; confirmed this is a deliberate, user-approved HTTP contract narrowing (FE impact discussed and confirmed acceptable before applying), not a silent breaking change.
- [x] 5.4 Verify in a running dev instance (`go run .`, using the AI Agent dev test account) that adding, listing, and removing a course collaborator through the API behaves identically to before the cutover. Done, **via a direct repository-level smoke test instead of HTTP**, because `DEV_TEST_ACCOUNT_EMAIL`/`DEV_TEST_ACCOUNT_PASSWORD` are not set in this shell (same blocker prior sessions hit — `.context/session-6d6e1382`/`7e8f43a7`; no credential was fabricated). Wired the real `CoursePolicyProvider`/`Authorizer`/`RoleBindingService`/`GormRepository` against the live dev DB and drove `ListCollaborators` → `AddCollaboratorsBulk` → `ListCollaborators` → `RemoveCollaborator` → `ListEditableCourses` → `ListInstructorCandidates` → re-add (idempotency of the "already active" path) end to end with a real course/owner/instructor-role user. Found and fixed one real bug this way: `ListEditableCourses`'s `cc.resource_id = c.id` join compared `VARCHAR` to `UUID` directly (worked with the old `course_collaborators.course_id UUID` column, breaks with `authorization_role_bindings.resource_id VARCHAR(128)`) — fixed with an explicit `c.id::text` cast. All scratch smoke-test files removed afterward; no test data left behind beyond the transient add/remove (owner/EDITOR state restored to before the test).

## 6. Drop course_collaborators

- [x] 6.0 Re-point `internal/appcli`'s legacy data importer's `course_collaborators` handling onto `authorization_role_bindings`: in `import_legacy_data_pipeline.go`'s `importLegacyStatement`, add a special case for `st.Table == "course_collaborators"` that builds an `INSERT INTO authorization_role_bindings` instead of the generic column-passthrough insert (see design.md's dedicated Decision for the exact column mapping: `principal_user_id`/`resource_id` via the existing `users`/`courses` ID maps, `resource_type = "course"`, skip `role == "OWNER"` rows, `revoked_at` from legacy `deleted_at`, `granted_by_user_id = NULL`). Add a test covering: a non-OWNER row is remapped and inserted correctly, an OWNER row is skipped, and a soft-deleted legacy row becomes an already-revoked binding. Confirmed with the user this importer is still in active future use, not retired. Done: `importLegacyCourseCollaboratorAsRoleBinding` + pure, unit-tested `legacyCourseCollaboratorRoleBindingArgs` (3 tests: OWNER-skip case-insensitive, correct arg mapping/order, `deleted_at`→`revoked_at` passthrough) added to `import_legacy_data_pipeline.go`. No prior test file existed for this package's import logic (only `cli_guard_test.go`), so this is also the first unit test coverage for any part of the legacy importer. `make check-all` passes clean.
- [x] 6.1 Confirm no remaining reference to `course_collaborators` exists anywhere in the repository (not only `internal/course/**` — task 6.0 covers the one reference genuinely outside it, `internal/appcli`), and verify with `go build ./...`. Done: found and fixed one last real dependency `requireCourseAction` (`repo_access.go`) still read `collaboratorRow` (a GORM model bound to `course_collaborators`) for the non-owner display Role. Replaced with a new `activeCollaboratorRoleBindingRow` reading `authorization_role_bindings` directly; verified live against the dev DB with a real EDITOR binding (`GetCourseDetail` as a non-owner returns `CollaboratorRole: "EDITOR"` sourced from the role gate). Also removed the now-dead `case *collaboratorRow:` branch in `ensureCourseRowID` (nothing inserts one anymore) and trimmed `collaboratorRow` itself down to a plain `{ID, UserID}` data-carrier struct with no `TableName()`/GORM tags (it now only survives as `existingActiveCollaboratorBindings`'s return shape, matching `planBulkCollaboratorWrites`'s existing test fixtures unchanged). The `constants.TableCourseCollaborators` string constant is left in place, to be removed alongside the `DROP TABLE` migration in 6.2 rather than now. `make check-all` passes clean.
- [x] 6.2 Write the migration dropping the `course_collaborators` table (up/down), and verify it applies cleanly against the dev database and the down migration restores the table shape. Irreversible-ish step: get explicit user go-ahead immediately before running it, per this project's own documented incident about not taking large-scope/destructive actions on an assumed decision (`.context/session-e56de0bc-...md`). Done, **with explicit user go-ahead obtained first**: migration `000037_drop_course_collaborators`. Hit and fixed one real bug during verification: the down migration's leading SQL comment contained a semicolon ("... in dev; authorization_role_bindings ...") which golang-migrate's naive multi-statement splitter (`MultiStatementEnabled: true`) mistook for a statement boundary, briefly leaving `schema_migrations` in a dirty state until corrected — rewrote the comment to avoid any semicolon, then re-verified up → down (table shape restored exactly, matching migration `000016`'s original DDL) → up (final state) all clean. Also removed the now-fully-dead `constants.TableCourseCollaborators` string constant.
- [x] 6.3 Run the full backend quality gate (`make check-all`, `make test-all`, `go build ./...`, `go vet ./...`) and confirm all pass. Done: clean. Also ran one more live smoke test post-drop (`ListCollaborators`/`AddCollaboratorsBulk`/`RemoveCollaborator` against the real dev DB with `course_collaborators` actually gone) to confirm nothing silently regressed.

## 7. Documentation and index sync

- [x] 7.1 Rewrite `docs/modules/authorization.md`'s "Registered providers" section (currently states Course does not use this module and that a future change may register it) to describe `CoursePolicyProvider` as registered, and extend the "Resource-scoped role gate" section with the wildcard `'*'` convention and the `RoleBindingService` write path. Replace the outdated text, do not leave it alongside the new text. Done: rewrote "Registered providers" (Course/`CoursePolicyProvider` described in full), extended "Resource-scoped role gate" with wildcard + `RoleBindingService` subsections, and corrected the now-stale "no production caller needs cross-repository transactions" line to describe the actual known gap (Course is that caller, and does not get atomicity).
- [x] 7.2 Update `docs/modules/course.md`: the Overview bullet listing "collaborator membership (`course_collaborators`)" and any section describing OWNER/EDITOR access checks, to describe collaborator membership as backed by `authorization_role_bindings`/`authorization_role_actions`; update its "Last audited" line. Done: Overview bullet, "Last audited" line, migration reference, "Collaboration and conflict control" (the stale "Course does not currently register a generic policy provider" line replaced), the collaborator-actions table, "Create course transaction" (step 4's `course_collaborators` insert removed — it never happened after this change), and the read-path performance table row all updated.
- [x] 7.3 Update `docs/database.md`: remove or annotate the `course_collaborators` table catalog entry (currently describes it as "current authorization source"), and add catalog/migration-list entries for the new backfill migration and the drop migration (alongside the existing `000034` row). Done: migration list gets rows for 000035/000036/000037; "Authorization tables" section updated to describe Course as the actual registered provider/writer (was "no provider is currently registered"); `course_collaborators` row in the Course management table struck through with a pointer to its replacement.
- [x] 7.4 Check `docs/modules/instructor.md` for any description of the instructor-candidate exclusion query (`instructorCandidatesBaseSQL`) and update it to name the new source table if present. Done: verified, no change needed — grep for `course_collaborators`/`instructorCandidates` finds nothing in this file; that query's behavior is documented in `docs/modules/course.md` instead (already updated in 7.2).
- [x] 7.5 Verify, rather than assume, that the HTTP contract is unchanged: compare `docs/course-collaboration-handoff-2026-06-04.md`, `docs/curl_api.md`, `docs/api_swagger.yaml`, `docs/return_types.md`, and `docs/router.md` against the actual request/response shapes after cutover, and update only the ones where the shape actually changed. If `docs/api_swagger.yaml` changes, run `ruby scripts/generate-apidog-postman.rb` and sync `docs/api-dog-import.json` per the Swagger and API Documentation Rule. Done: `docs/curl_api.md` and `docs/router.md` verified accurate as-is (HTTP contract unchanged, no edit). `docs/return_types.md` and `docs/api_swagger.yaml` fixed (stale `course_collaborators` insert claim; collaborator-list ordering description). `docs/course-collaboration-handoff-2026-06-04.md` given a new dated addendum (table drop, `RoleBindingService`) and its stale `repo_collaborators_bulk.go` internals bullet corrected. Since `docs/api_swagger.yaml` changed, ran `ruby scripts/generate-apidog-postman.rb`; `docs/api-dog-import.json` regenerated and now staged.
- [x] 7.6 Grep `docs/sequence_diagrams.md`, `docs/logic-flow.md`, `docs/modules.md`, `docs/api-overview.md`, and `docs/data-flow.md` for `collaborator`/`course_collaborators` mentions, and update any that describe the old mechanism as still current. Done: `docs/sequence_diagrams.md`'s Create Course diagram fixed (removed the `INSERT course_collaborators (OWNER)` step and the `requireCourseAccess (courses + collaborators JOIN)` description; now shows owner synthesis via `requireCourseAction`/`Authorizer`). `docs/logic-flow.md`'s Course/Instructor Logic bullet fixed (was claiming "the generic `internal/authorization` module currently has no Course provider" and an active `course_collaborators` membership; now describes `requireCourseAction`/`CoursePolicyProvider`/`authorization_role_bindings`). `docs/modules.md`, `docs/api-overview.md`, `docs/data-flow.md` checked — all mentions are generic/HTTP-level descriptions unaffected by the cutover, no change needed.
- [ ] 7.7 Run GitNexus re-index (`npx gitnexus analyze`) after the final commit and confirm the index reflects the new/changed symbols (`CoursePolicyProvider`, `RoleBindingService`, the collapsed access-check seam).
- [x] 7.8 (added, user-requested full-repo doc sweep after the fourth round of fixes: "update hết toàn bộ docs đi") Grepped every file under `docs/` for `internal/authorization`/`CoursePolicyProvider`/`RoleBindingService`/`authorization_role_bindings`/`course_collaborators`/`collaborator`/`role.gate`/`role.binding`/`GrantService`, not just the files touched by earlier rounds. Found and fixed several gaps 7.1-7.6 missed entirely: `docs/reusable-assets.md`'s "Collaborator bulk add" entry still described the pre-role-gate implementation verbatim (`applyBulkCollaboratorWrites`, `UPDATE id IN (…)`, `GrantService.ReplaceMany`, `collaboratorRow`) — rewritten to match the current `RoleBindingService`-based, single-transaction implementation. `docs/modules.md`'s Authorization entry still said "No provider or runtime consumer is currently registered" and its Course entry described a "generic scoped grants for basic info and outline" mechanism that was actually the prior, reverted design — both rewritten; also added its migration list note. `docs/dependencies.md`'s bounded-context list and `docs/architecture.md`'s bounded-context table were both missing `internal/authorization` entirely (present in the repo since migration `000034`, well before this change) — added. `docs/architecture.md`'s Composition Root section had a stale, wrong service-construction order and no mention of `wireAuthorization`/`wireCourse` at all — corrected against `wire.go`'s actual call order. `docs/requirements.md`'s FR-8 bulk-collaborator requirement still described `UPDATE … WHERE id IN (?)` + `CreateInBatches` — corrected to `RoleBindingService.Assign`. `docs/modules/authorization.md` gained two missing behavior notes: `ListRoleBindings`/`List` matching wildcard bindings for a concrete resource (while `RevokeRoleBindings` deliberately does not), and `requireCourseAction`'s own display-role lookup applying the same wildcard match. `docs/modules/course.md`'s bulk-add row updated to state the `role` field is `EDITOR`-only and that re-adding an existing collaborator is a no-op (never a role change). `docs/course-collaboration-handoff-2026-06-04.md` given a new dated addendum summarizing the transaction/wildcard/DTO fixes. Docs-only change; no code touched, so no quality-gate re-run needed beyond confirming this with `git status`/`git diff --stat`.
