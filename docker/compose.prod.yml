# Docker Compose — STAGE=prod (host port 8080)
name: mycourse-be-prod

services:
  api:
    build:
      context: ..
      dockerfile: Dockerfile
      args:
        STAGE: prod
    image: mycourse-io-be:prod
    env_file:
      - ../.env
      - ../.env.prod
    environment:
      STAGE: prod
      CGO_ENABLED: "1"
      MIGRATE: "1"
    ports:
      - "8080:8080"
    healthcheck:
      test: ["CMD", "curl", "-fsS", "http://127.0.0.1:8080/api/v1/health"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 30s

  # --- OPTIONAL local infra (commented out) ---
  # postgres:
  #   image: postgres:16-alpine
  # redis:
  #   image: redis:7-alpine
