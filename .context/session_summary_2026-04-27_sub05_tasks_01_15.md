# Phase Sub 05 Tasks 01-15 summary (2026-04-27)

- Re-ran GitNexus index + status and completed impact analysis (all target symbols LOW risk) before edits:
  - `CreateFile`, `UploadB2`, `UploadBunnyVideo`, `BuildPublicURL`, `HandleBunnyVideoWebhook`, `GetVideoStatus`, `RegisterWebhookRoutes`, `applyYAMLToGlobals`.
- Locked upload scope:
  - file branch: B2 + Gcore CDN URL `<cdn>/<bucket>/<file>`
  - video branch: Bunny Stream (`CreateVideo -> UploadVideoContent`) + status polling + webhook route outside auth.
- Added media metadata persistence:
  - migration: `migrations/000003_media_metadata.up.sql`, `.down.sql`
  - dbschema: `dbschema/media_namespace.go`, `dbschema/media_files.go`
  - model: `models/media_file.go`
  - repository: `repository/media/file_repository.go`
  - root wiring: `repository/repository.go`
- Updated service workflows:
  - `services/media/file_service.go` now persists create/update metadata, lists/gets from DB, soft-deletes row after successful cloud delete, and keeps fallback for legacy object keys not yet persisted.
  - `services/media/video_service.go` webhook now syncs finished Bunny metadata/duration to DB idempotently.
- Config and env sync:
  - added `media.app_media_provider` in all `config/app*.yaml`.
  - added `MEDIA_APP_PROVIDER` in `.env*.example`.
  - bucket still comes from env and is resolved through `setting.MediaSetting.B2Bucket`.
- Response contract extension:
  - `dto.UploadFileResponse` + mapper/entity now include persisted metadata fields (`id`, `kind`, `provider`, `filename`, `mime_type`, `size_bytes`, `status`, `b2_bucket_name`, Bunny IDs, duration, video provider).
- Validation:
  - `gofmt -w ...` on all changed Go files.
  - `go test ./...` passed.
