# Copy → `.env.staging` khi STAGE=staging (config/app-staging.yaml).

SERVER_RUN_MODE=release
# SERVER_PORT=8080
AUTO_SYNC_PERMISSION_JOB=false
AUTO_SYNC_ROLE_PERMISSION_JOB=false

# DATABASE_URL=postgres://...
# DB_SSLMODE=require
