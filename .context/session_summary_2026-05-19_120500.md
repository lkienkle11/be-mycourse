# Session Summary

> Saved: 2026-05-19 12:05:00
> Branch: docs/update-plan-v1
> Project: be-mycourse

## Overview
Register confirmation email links now include FE locale path. `POST /auth/register` accepts optional `locale` (`en`|`vi`, default `vi`).

## Completed
- [x] `RegisterRequest.locale` + `normalizeRegisterLocale` in delivery
- [x] `AuthService.Register` / `sendRegistrationEmail` use `/{locale}/confirm-email?token=`
- [x] `go fmt`, `go test ./internal/auth/...`, `go build ./...`
- [x] Updated `docs/modules/auth.md`, `docs/curl_api.md`

## Key decisions
- URL: `fmt.Sprintf("%s/%s/confirm-email?token=%s", baseURL, locale, token)` with trimmed `AppClientBaseURL`
- Confirm API contract unchanged (`POST /auth/confirm` JSON body)

## Next steps
- Deploy BE before or with FE so new email links resolve
- FE confirm page consumes token (implemented in fe-mycourse session)
